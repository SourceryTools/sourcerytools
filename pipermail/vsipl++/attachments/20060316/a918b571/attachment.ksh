2006-03-16  Don McCoy  <don@codesourcery.com>

	* benchmarks/make.standalone: Fixed a bug where it would
	  not recognize that PREFIX was set on the command line.
	  Fixed include paths and build targets.
