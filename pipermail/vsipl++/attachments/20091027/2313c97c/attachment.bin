Index: src/vpp_source-trunk/doc/reference-manual/chol.xml
===================================================================
--- src/vpp_source-trunk/doc/reference-manual/chol.xml	(revision 266493)
+++ src/vpp_source-trunk/doc/reference-manual/chol.xml	(working copy)
@@ -185,7 +185,8 @@
      <para>If <code>tr == mat_herm</code> and <type>T</type> is a 
      specialization of complex, then 
      <code>A<superscript>H</superscript>X = B</code> is solved.</para>
-     <para>Otherwise, <code>AX = B</code> is solved.</para>
+     <para>If <code>tr == mat_ntrans</code>, then
+     <code>AX = B</code> is solved.</para>
 
    <formalpara>
      <title>Requirements:</title>
@@ -239,7 +240,8 @@
      <para>If <code>tr == mat_herm</code> and <type>T</type> is a 
      specialization of complex, then 
      <code>A<superscript>H</superscript>X = B</code> is solved.</para>
-     <para>Otherwise, <code>AX = B</code> is solved.</para>
+     <para>If <code>tr == mat_ntrans</code>, then
+     <code>AX = B</code> is solved.</para>
    
    <formalpara>
      <title>Requirements:</title>
Index: src/vpp_source-trunk/doc/reference-manual/lu.xml
===================================================================
--- src/vpp_source-trunk/doc/reference-manual/lu.xml	(revision 266493)
+++ src/vpp_source-trunk/doc/reference-manual/lu.xml	(working copy)
@@ -168,7 +168,8 @@
      <para>If <code>tr == mat_herm</code> and <type>T</type> is a 
      specialization of complex, then 
      <code>A<superscript>H</superscript>X = B</code> is solved.</para>
-     <para>Otherwise, <code>AX = B</code> is solved.</para>
+     <para>If <code>tr == mat_ntrans</code>, then
+     <code>AX = B</code> is solved.</para>
 
    <formalpara>
      <title>Requirements:</title>
@@ -220,7 +221,8 @@
      <para>If <code>tr == mat_herm</code> and <type>T</type> is a 
      specialization of complex, then 
      <code>A<superscript>H</superscript>X = B</code> is solved.</para>
-     <para>Otherwise, <code>AX = B</code> is solved.</para>
+     <para>If <code>tr == mat_ntrans</code>, then
+     <code>AX = B</code> is solved.</para>
    
    <formalpara>
      <title>Requirements:</title>
Index: src/vpp_source-trunk/doc/reference-manual/qr.xml
===================================================================
--- src/vpp_source-trunk/doc/reference-manual/qr.xml	(revision 266493)
+++ src/vpp_source-trunk/doc/reference-manual/qr.xml	(working copy)
@@ -387,7 +387,8 @@
      <para>If <code>tr == mat_herm</code> and <type>T</type> is a specialization 
      of complex, then 
      <code>R<superscript>H</superscript>X = alpha B</code> is solved.</para>
-     <para>Otherwise, <code>RX = alpha B</code> is solved.</para>
+     <para>If <code>tr == mat_ntrans</code>, then
+     <code>RX = alpha B</code> is solved.</para>
 
    <formalpara>
      <title>Requirements:</title>
@@ -742,7 +743,8 @@
      <para>If <code>tr == mat_herm</code> and <type>T</type> is a specialization 
      of complex, then 
      <code>R<superscript>H</superscript>X = alpha B</code> is solved.</para>
-     <para>Otherwise, <code>RX = alpha * B</code> is solved.</para>
+     <para>If <code>tr == mat_ntrans</code>, then
+     <code>RX = alpha B</code> is solved.</para>
    
    <formalpara>
      <title>Requirements:</title>
